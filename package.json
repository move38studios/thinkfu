{
  "name": "thinkfu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "validate": "pnpm tsx scripts/validate-catalog.ts",
    "build:catalog": "pnpm tsx scripts/build-catalog-bundle.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "yaml": "^2.8.3"
  }
}
